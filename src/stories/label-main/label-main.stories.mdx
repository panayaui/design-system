import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
import LabelMainComponent from './label-main.component';
import {LabelTypesEnum} from './label-main.enum';

<Meta title='Components/Label (Status)/Main'
  component={LabelMainComponent}
 />

# Label (Status) Main

export const Template = (args) => <LabelMainComponent {...args} />

###### Selector

```html
  <p-label-main></p-label-main>
```
###### Types
<Canvas>
  <p className="story-title">critical</p>
  <Story name="Critical"
    args={{ type: LabelTypesEnum.CRITICAL, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">danger</p>
  <Story name="Danger"
    args={{ type: LabelTypesEnum.DANGER, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">success</p>
   <Story name="Success"
    args={{ type: LabelTypesEnum.SUCCESS, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">medium</p>
   <Story name="Medium"
    args={{ type: LabelTypesEnum.MEDIUM, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">low</p>
   <Story name="Low"
    args={{ type: LabelTypesEnum.LOW, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">light gray</p>
   <Story name="Light Gray"
    args={{ type: LabelTypesEnum.LIGHTGRAY, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">dark gray</p>
   <Story name="Dark Gray"
    args={{ type: LabelTypesEnum.DARKGRAY, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">dev1</p>
   <Story name="Dev1"
    args={{ type: LabelTypesEnum.DEV1, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">dev2</p>
   <Story name="Dev2"
    args={{ type: LabelTypesEnum.DEV2, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">dev3</p>
  <Story name="Dev3"
    args={{ type: LabelTypesEnum.DEV3, label: 'Label' }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### Types Enum

```ts
  export enum LabelTypesEnum {
  CRITICAL = 'critical',
  DANGER = 'danger',
  SUCCESS = 'success',
  MEDIUM = 'medium',
  LOW = 'low',
  LIGHTGRAY = 'lightGray',
  DARKGRAY = 'darkGray',
  DEV1 = 'dev1',
  DEV2= 'dev2',
  DEV3 = 'dev3'
}
```