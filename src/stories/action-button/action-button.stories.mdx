import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
import {ActionButtonComponent} from 'projects/design-system-lib/src/public-api';
import {ActionButtonModule} from 'projects/design-system-lib/src/public-api';

<Meta title='Components/Action Button'
  component={ActionButtonComponent}
  decorators={[
    moduleMetadata({
      imports: [ActionButtonModule]
    })
  ]}
  args={{
    label: 'Action item',
    iconBeforeName: 'trash-2',
    iconBeforeWidth: '17',
    isMenuTrigger: true,
    isStandalone: true
  }}
/>

export const Template = (args) => <ActionButtonComponent {...args} />

# Toolbar Button

###### Selector
```html
  <p-toolbar-button></p-toolbar-button>
```

###### Standalone
<Canvas>
  <Story name="Action Button"
    args={{
      isMenuTrigger: false
    }}>
    {Template.bind({})}
  </Story>
   <Story name="Menu Trigger"
    args={{
    }}>
    {Template.bind({})}
  </Story>
   <Story name="Action Button Disabled"
    args={{
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### In the Toolbar
<Canvas>
  <Story name="Toolbar Button"
    args={{
      isMenuTrigger: false,
      isStandalone: false
    }}>
    {Template.bind({})}
  </Story>
   <Story name="Toolbar Menu Trigger"
    args={{
      isStandalone: false
    }}>
    {Template.bind({})}
  </Story>
   <Story name="Toolbar Button Disabled"
    args={{
      disabled: true,
      isStandalone: false
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### Component
```ts
export class ActionButtonComponent {
  @Input() label: string;
  @Input() disabled: boolean = false;
  @Input() iconBeforeName: string;
  @Input() iconBeforeWidth: string = 'auto';
  @Input() iconBeforeHeight: string = 'auto';
  @Input() iconBeforeStrokeWidth: number;
  @Input() iconBeforeStrokeColor: string;
  @Input() isMenuTrigger: boolean = false;
  public isActive: boolean = false;
}
```

###### IActionButton
```ts
export interface IActionButton {
  label: string;
  disabled?: boolean;
  iconBeforeName?: string;
  iconBeforeWidth?: string;
  iconBeforeHeight?: string;
  iconBeforeStrokeWidth?: number;
  iconBeforeStrokeColor?: string;
  isMenuTrigger?: boolean;
  isStandalone?: boolean;
}
```
