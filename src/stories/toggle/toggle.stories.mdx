import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
// import {ToggleComponent} from 'design-system-lib';
// import {ButtonModule} from 'design-system-lib';
import {ToggleComponent} from 'projects/design-system-lib/src/public-api';
import {IconModule} from 'projects/design-system-lib/src/lib/icon/icon.module';

<Meta title='Toggle'
  component={ToggleComponent}
  decorators={[
    moduleMetadata({
      imports: [
        IconModule
      ]
    })
  ]}  
/>

export const Template = (args) => <ToggleComponent {...args} />

# Toggle Button

###### Selector
```ts
  <p-toggle></p-toggle>
```

###### Label
<Canvas>
  <Story name="Two Options"
    args={{
      optionsListName: 'twoLabelsToggle',
      optionsList: [{
        optionName: 'First',
        checked: true
      },
      {
        optionName: 'Second'
      }]
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Three Options"
    args={{
      optionsListName: 'threeLabelsToggle',
      optionsList: [{
        optionName: 'First',
        checked: true
      },
      {
        optionName: 'Second'
      },
      {
        optionName: 'Third'
      }]
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Two Options Disabled"
    args={{
      optionsListName: 'twoLabelsToggleDisabled',
      optionsList: [{
        optionName: 'First',
        checked: true
      },
      {
        optionName: 'Second'
      }],
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Three Options Disabled"
    args={{
      optionsListName: 'threeLabelsToggleDisabled',
      optionsList: [{
        optionName: 'First',
        checked: true
      },
      {
        optionName: 'Second'
      },
      {
        optionName: 'Third'
      }],
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### Icon
<Canvas>
  <Story name="Icons Two Options"
    args={{
      optionsListName: 'twoIconsToggle',
      optionsList: [{
        optionName: 'First',
        icon: { 
          iconName: 'grid',        
          height: 'auto'
        },
        checked: true
      },
      {
        optionName: 'Second',
        icon: { 
          iconName: 'list',         
          height: 'auto'
        }
      }]
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Icons Three Options"
    args={{
      optionsListName: 'threeIconsToggle',
      optionsList: [{
        optionName: 'First',
        icon:  { 
          iconName: 'grid',        
          height: 'auto'
        },
        checked: true
      },
      {
        optionName: 'Second',
        icon:  { 
          iconName: 'grid',
          height: 'auto'
        }
      },
      {
        optionName: 'Third',
        icon:  { 
          iconName: 'list',        
          height: 'auto'
        }
      }]
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Icons Two Options Disabled"
    args={{
      optionsListName: 'twoIconsToggleDisabled',
      optionsList: [{
        optionName: 'First',
        icon: { 
          iconName: 'grid',        
          height: 'auto'
        },
        checked: true
      },
      {
        optionName: 'Second',
        icon: { 
          iconName: 'list',         
          height: 'auto'
        }
      }],
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Icons Three Options Disabled"
    args={{
      optionsListName: 'threeIconsToggleDisabled',
      optionsList: [{
        optionName: 'First',
        icon:  { 
          iconName: 'grid',        
          height: 'auto'
        },
        checked: true
      },
      {
        optionName: 'Second',
        icon:  { 
          iconName: 'grid',
          height: 'auto'
        }
      },
      {
        optionName: 'Third',
        icon:  { 
          iconName: 'list',        
          height: 'auto'
        }
      }],
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### Component
```ts
export class ToggleComponent {
  @Input() optionsListName: string;
  @Input() optionsList: IToggleOption[];
  @Input() disabled: boolean;
}
```

###### IToggleOption
```ts
interface IToggleOption {
  optionName: string;
  icon: IIcon;
  checked: boolean;
}
```

###### IIcon
```ts
export interface IIcon {
  iconName: string; // required
  width: string;
  height: string;
  fill: string;
  stroke: string;
  strokeWidth: number;
  strokeLinecap: string;
  strokeLinejoin: string;
}
```