import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
import {ChipButtonComponent} from 'projects/design-system-lib/src/public-api';
import {ChipsModule} from 'projects/design-system-lib/src/public-api';

<Meta title='Components/Chips/Buttons'
  component={ChipButtonComponent}
  decorators={[
    moduleMetadata({
      imports: [
        ChipsModule
      ]
    })
  ]}
  args={{
    label: 'Type',
    iconRemove: true,
    values: ['Unassaigned', 'Released', 'Something very very long long']
  }}
/>

export const Template = (args) => <ChipButtonComponent {...args} />

# Chip Button

###### Selector
```ts
// for standalone use
<p-chip-button></p-chip-button>

// to show a line of chips 
<p-chips></p-chips>
```
###### With Remove Icon
<Canvas>
  <p className="story-title">No filter Default</p>
  <Story name="No filter Default"
    args={{ 
      values: ['All']
     }}>
    {Template.bind({})}
  </Story>
   <p className="story-title">No filter Clicked</p>
  <Story name="No filter Clicked"
    args={{ 
      active: true,
      values: ['All'] }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">With filter Default</p>
  <Story name="With filter Default"
    args={{ full: true }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">With filter Clicked</p>
  <Story name="With filter Clicked"
    args={{ full: true, active: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### No Remove Icon
<Canvas>
  <p className="story-title">With filter Default</p>
  <Story name="With filter Default No Remove"
    args={{ iconRemove: false }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">With filter Clicked</p>
  <Story name="With filter Clicked No Remove"
    args={{ iconRemove: false, full: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### IChip
```ts
export interface IChip {
  label: string;
  values?: string[];
  iconRemove?: boolean;
  full?: boolean;
  active?: boolean;
}
```