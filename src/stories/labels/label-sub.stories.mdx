import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
import {LabelSubComponent} from 'projects/design-system-lib/src/public-api';
import {LabelTypesEnum} from 'projects/design-system-lib/src/public-api';

<Meta title='Components/Label (Status)/Sub'
  component={LabelSubComponent}
 />

# Label (Status) Sub

export const Template = (args) => <LabelSubComponent {...args} />

###### Selector

```html
  <p-label-sub></p-label-sub>
```
###### Types
<Canvas>
  <p className="story-title hidden-text">type</p>
  <p className="story-title">Label On</p>
  <p className="story-title">Label Off</p>
  <p className="story-title">critical</p>
  <Story name="Critical"
    args={{ type: LabelTypesEnum.CRITICAL, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <Story name="Critical No Label"
    args={{ type: LabelTypesEnum.CRITICAL, ariaLabel: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">danger</p>
  <Story name="Danger"
    args={{ type: LabelTypesEnum.DANGER, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <Story name="Danger No Label"
    args={{ type: LabelTypesEnum.DANGER, ariaLabel: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">success</p>
  <Story name="Success"
    args={{ type: LabelTypesEnum.SUCCESS, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <Story name="Success No Label"
    args={{ type: LabelTypesEnum.SUCCESS, ariaLabel: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">medium</p>
  <Story name="Medium"
    args={{ type: LabelTypesEnum.MEDIUM, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <Story name="Medium No Label"
    args={{ type: LabelTypesEnum.MEDIUM, ariaLabel: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">low</p>
  <Story name="Low"
    args={{ type: LabelTypesEnum.LOW, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <Story name="Low No Label"
    args={{ type: LabelTypesEnum.LOW, ariaLabel: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">light gray</p>
  <Story name="Light Gray"
    args={{ type: LabelTypesEnum.LIGHTGRAY, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <Story name="Light Gray No Label"
    args={{ type: LabelTypesEnum.LIGHTGRAY, ariaLabel: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">dark gray</p>
  <Story name="Dark Gray"
    args={{ type: LabelTypesEnum.DARKGRAY, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <Story name="Dark Gray No Label"
    args={{ type: LabelTypesEnum.DARKGRAY, ariaLabel: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">dev1</p>
  <Story name="Dev1"
    args={{ type: LabelTypesEnum.DEV1, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <Story name="Dev1 No Label"
    args={{ type: LabelTypesEnum.DEV1, ariaLabel: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">dev2</p>
  <Story name="Dev2"
    args={{ type: LabelTypesEnum.DEV2, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <Story name="Dev2 No Label"
    args={{ type: LabelTypesEnum.DEV2, ariaLabel: 'Label' }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">dev3</p>
  <Story name="Dev3"
    args={{ type: LabelTypesEnum.DEV3, label: 'Label' }}>
    {Template.bind({})}
  </Story>
  <Story name="Dev3 No Label"
    args={{ type: LabelTypesEnum.DEV3, ariaLabel: 'Label' }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### Component
```ts
export class LabelSubComponent {
  @Input() label: string;
  @Input() ariaLabel: string; // required if there is no label
  @Input() type: LabelTypesEnum;
}
```

###### Types Enum
```ts
  export enum LabelTypesEnum {
  CRITICAL = 'critical',
  DANGER = 'danger',
  SUCCESS = 'success',
  MEDIUM = 'medium',
  LOW = 'low',
  LIGHTGRAY = 'lightGray',
  DARKGRAY = 'darkGray',
  DEV1 = 'dev1',
  DEV2= 'dev2',
  DEV3 = 'dev3'
}
```
