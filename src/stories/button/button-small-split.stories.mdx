import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';

import {SplitButtonComponent} from 'projects/design-system-lib/src/public-api';
import {ButtonTypeEnum} from 'projects/design-system-lib/src/public-api';
import {ButtonModule} from 'projects/design-system-lib/src/public-api';

<Meta title='Components/Button/Split'
  component={SplitButtonComponent}
  decorators={[
    moduleMetadata({
      imports: [ButtonModule]
    })
  ]}
/>

export const Template = (args) => <SplitButtonComponent {...args} />

# Button Split

###### Selector
```ts
  <p-split-button></p-split-button>
```

###### Small
<Canvas>
  <p className="story-title">Default</p>
  <Story name="Primary"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Primary
    }}>
    {Template.bind({})}
  </Story>
    <Story name="Secondary"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Secondary
    }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">Disabled</p>
  <Story name="Primary Disabled"
    args={{
      label: 'Label',
      disabled: true,
      buttonType: ButtonTypeEnum.Primary
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary Disabled"
    args={{
      label: 'Label',
      disabled: true,
      buttonType: ButtonTypeEnum.Secondary
    }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">Icon</p>
  <Story name="Primary With Icon"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Primary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary With Icon"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Secondary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">Disabled</p>
  <Story name="Primary With Icon Disabled"
    args={{
      label: 'Label',
      disabled: true,
      buttonType: ButtonTypeEnum.Primary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary With Icon Disabled"
    args={{
      label: 'Label',
      disabled: true,
      buttonType: ButtonTypeEnum.Secondary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### Large
<Canvas>
  <p className="story-title">Default</p>
  <Story name="Large Primary"
    args={{ 
      label: 'Label',
      large: true,
      buttonType: ButtonTypeEnum.Primary
    }}>
    {Template.bind({})}
  </Story>
    <Story name="Large Secondary"
    args={{
      label: 'Label',
      large: true,
      buttonType: ButtonTypeEnum.Secondary
    }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">Disabled</p>
  <Story name="Large Primary Disabled"
    args={{
      label: 'Label',
      disabled: true,
      large: true,
      buttonType: ButtonTypeEnum.Primary
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Large Secondary Disabled"
    args={{
      label: 'Label',
      disabled: true,
      large: true,
      buttonType: ButtonTypeEnum.Secondary
    }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">Icon</p>
  <Story name="Large Primary With Icon"
    args={{
      label: 'Label',
      large: true,
      buttonType: ButtonTypeEnum.Primary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Large Secondary With Icon"
    args={{
      label: 'Label',
      large: true,
      buttonType: ButtonTypeEnum.Secondary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <p className="story-title">Disabled</p>
  <Story name="Large Primary With Icon Disabled"
    args={{
      label: 'Label',
      disabled: true,
      large: true,
      buttonType: ButtonTypeEnum.Primary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Large Secondary With Icon Disabled"
    args={{
      label: 'Label',
      disabled: true,
      large: true,
      buttonType: ButtonTypeEnum.Secondary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### Component
```ts
export class ButtonComponent {
  @Input() label: string;
  @Input() large: boolean = false;
  @Input() disabled: boolean = false;
  @Input() iconBefore: string;
  @Input() iconBeforeWidth: string = 'auto';
  @Input() iconBeforeHeight: string = 'auto';
  @Input() iconBeforeStrokeWidth: number;
  @Input() iconAfter: string;
  @Input() iconAfterWidth: string = 'auto';
  @Input() iconAfterHeight: string = 'auto';
  @Input() iconAfterStrokeWidth: number;
  @Input() buttonType: ButtonTypeEnum = ButtonTypeEnum.Primary;
  @Output() onClickTask: EventEmitter<any> = new EventEmitter();
}
export class SplitButtonComponent extends ButtonComponent {
  @Output() onClickTrigger: EventEmitter<any> = new EventEmitter();
}
```

###### Types
```ts
export enum ButtonTypeEnum {
  Primary = 'primary',
  Secondary = 'secondary',
  Tertiary = 'tertiary',
  Distructive = 'distructive'
}
```
