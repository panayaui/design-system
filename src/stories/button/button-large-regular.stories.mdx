import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
// import {ButtonComponent} from 'design-system-lib';
// import {ButtonModule} from 'design-system-lib';
import {ButtonComponent} from 'projects/design-system-lib/src/public-api';
import {ButtonTypeEnum} from 'projects/design-system-lib/src/public-api';
import {IconModule} from 'projects/design-system-lib/src/lib/icon/icon.module';

<Meta title='Components/Button/Large/Regular'
  component={ButtonComponent}
  decorators={[
    moduleMetadata({
      imports: [IconModule]
    })
  ]}
  args={{ onClickTask: e => {
        e.preventDefault();
        console.log(e.target);
      } }}
/>

export const Template = (args) => <ButtonComponent {...args} />

# Button Large

###### Selector
```ts
  <p-button [large]="true"></p-button>
```

###### Default
<Canvas>
  <p className="story-title">Primary</p>
  <p className="story-title">Secondary</p>
  <p className="story-title">Tertiary</p>
  <p className="story-title">Distructive</p>
  <Story name="Primary"
    args={{
      label: 'Label',
      large: true,
      buttonType: ButtonTypeEnum.Primary
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary"
    args={{
      label: 'Label',
      large: true,
      buttonType: ButtonTypeEnum.Secondary
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Tertiary"
    args={{
      label: 'Label',
      large: true,
      buttonType: ButtonTypeEnum.Tertiary
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Distructive"
      args={{
        label: 'Label',
        large: true,
        buttonType: ButtonTypeEnum.Distructive
      }}>
      {Template.bind({})}
    </Story>
</Canvas>

###### Disabled
<Canvas>
  <p className="story-title">Primary</p>
  <p className="story-title">Secondary</p>
  <p className="story-title">Tertiary</p>
  <p className="story-title">Distructive</p>
  <Story name="Primary Disabled"
    args={{
      label: 'Label',
      large: true,
      disabled: true,
      buttonType: ButtonTypeEnum.Primary
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary Disabled"
    args={{
      label: 'Label',
      large: true,
      disabled: true,
      buttonType: ButtonTypeEnum.Secondary
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Tertiary Disabled"
    args={{
      label: 'Label',
      large: true,
      disabled: true,
      buttonType: ButtonTypeEnum.Tertiary
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Distructive Disabled"
      args={{
        label: 'Label',
        large: true,
        disabled: true,
        buttonType: ButtonTypeEnum.Distructive
      }}>
      {Template.bind({})}
    </Story>
</Canvas>

###### Component
```ts
export class ButtonComponent {
  @Input() label: string;
  @Input() large: boolean = false;
  @Input() disabled: boolean = false;
  @Input() iconBefore: string;
  @Input() iconBeforeWidth: string = 'auto';
  @Input() iconBeforeHeight: string = 'auto';
  @Input() iconBeforeStrokeWidth: number;
  @Input() iconAfter: string;
  @Input() iconAfterWidth: string = 'auto';
  @Input() iconAfterHeight: string = 'auto';
  @Input() iconAfterStrokeWidth: number;
  @Input() buttonType: ButtonTypeEnum = ButtonTypeEnum.Primary;
  @Output() onClickTask: EventEmitter<any> = new EventEmitter();
}
```

###### Types
```ts
export enum ButtonTypeEnum {
  Primary = 'primary',
  Secondary = 'secondary',
  Tertiary = 'tertiary',
  Distructive = 'distructive'
}
```
