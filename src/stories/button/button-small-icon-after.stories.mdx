import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
// import {ButtonComponent} from 'design-system-lib';
// import {ButtonModule} from 'design-system-lib';
import {ButtonComponent} from 'projects/design-system-lib/src/public-api';
import {ButtonTypeEnum} from 'projects/design-system-lib/src/public-api';
import {IconModule} from 'projects/design-system-lib/src/lib/icon/icon.module';

<Meta title='Components/Button/Small/Icons/After'
  component={ButtonComponent}
  decorators={[
    moduleMetadata({
      imports: [IconModule]
    })
  ]}  
/>

export const Template = (args) => <ButtonComponent {...args} />

# Button Small With Icon After

###### Selector
```ts
  <p-button></p-button>
```

###### Default
<Canvas>
  <p className="story-title">Primary</p>
  <p className="story-title">Secondary</p>
  <p className="story-title">Tertiary</p>
  <p className="story-title">Distructive</p>
  <Story name="Primary Icon After"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Primary,
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary Icon After"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Secondary,
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Tertiary Icon After"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Tertiary,
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Distructive Icon After"
      args={{
        label: 'Label',
        buttonType: ButtonTypeEnum.Distructive,
        iconAfter: 'chevron-down',
        iconAfterWidth: '17'
      }}>
      {Template.bind({})}
    </Story>
</Canvas>

###### Disabled
<Canvas>
  <p className="story-title">Primary</p>
  <p className="story-title">Secondary</p>
  <p className="story-title">Tertiary</p>
  <p className="story-title">Distructive</p>
  <Story name="Primary Icon After Disabled"
    args={{
      label: 'Label',
      disabled: true,
      buttonType: ButtonTypeEnum.Primary,
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary Icon After Disabled"
    args={{
      label: 'Label',
      disabled: true,
      buttonType: ButtonTypeEnum.Secondary,
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Tertiary Icon After Disabled"
    args={{
      label: 'Label',
      disabled: true,
      buttonType: ButtonTypeEnum.Tertiary,
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Distructive Icon After Disabled"
      args={{
        label: 'Label',
        disabled: true,
        buttonType: ButtonTypeEnum.Distructive,
        iconAfter: 'chevron-down',
        iconAfterWidth: '17'
      }}>
      {Template.bind({})}
    </Story>
</Canvas>

###### Component
```ts
export class ButtonComponent {
  @Input() label: string;
  @Input() large: boolean = false;
  @Input() disabled: boolean = false;
  @Input() iconAfter: string;
  @Input() iconAfterWidth: string = 'auto';
  @Input() iconAfterHeight: string = 'auto';
  @Input() iconAfter: string;
  @Input() iconAfterWidth: string = 'auto';
  @Input() iconAfterHeight: string = 'auto';
  @Input() buttonType: ButtonTypeEnum = ButtonTypeEnum.Primary;
}
```

###### Types
```ts
export enum ButtonTypeEnum {
  Primary = 'primary',
  Secondary = 'secondary',
  Tertiary = 'tertiary',
  Distructive = 'distructive'
}
```
