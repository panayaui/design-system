import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
// import {ButtonComponent} from 'design-system-lib';
// import {ButtonModule} from 'design-system-lib';
import {ButtonComponent} from 'projects/design-system-lib/src/public-api';
import {ButtonTypeEnum} from 'projects/design-system-lib/src/public-api';
import {IconModule} from 'projects/design-system-lib/src/lib/icon/icon.module';

<Meta title='Components/Button/Small/Icons/Before&After'
  component={ButtonComponent}
  decorators={[
    moduleMetadata({
      imports: [IconModule]
    })
  ]}  
/>

export const Template = (args) => <ButtonComponent {...args} />

# Button Small With Icon Before & After

###### Selector
```ts
  <p-button></p-button>
```

###### Default
<Canvas>
  <p className="story-title">Primary</p>
  <p className="story-title">Secondary</p>
  <p className="story-title">Tertiary</p>
  <p className="story-title">Distructive</p>
  <Story name="Primary Icon Before & After"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Primary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary Icon Before & After"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Secondary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Tertiary Icon Before & After"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Tertiary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Distructive Icon Before & After"
      args={{
        label: 'Label',
        buttonType: ButtonTypeEnum.Distructive,
        iconBefore: 'eye',
        iconBeforeWidth: '17',
        iconAfter: 'chevron-down',
        iconAfterWidth: '17'
      }}>
      {Template.bind({})}
    </Story>
</Canvas>

###### Disabled
<Canvas>
  <p className="story-title">Primary</p>
  <p className="story-title">Secondary</p>
  <p className="story-title">Tertiary</p>
  <p className="story-title">Distructive</p>
  <Story name="Primary Icon Before & After Disabled"
    args={{
      label: 'Label',
      disabled: true,
      buttonType: ButtonTypeEnum.Primary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary Icon Before & After Disabled"
    args={{
      label: 'Label',
      disabled: true,
      buttonType: ButtonTypeEnum.Secondary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Tertiary Icon Before & After Disabled"
    args={{
      label: 'Label',
      disabled: true,
      buttonType: ButtonTypeEnum.Tertiary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="Distructive Icon Before & After Disabled"
      args={{
        label: 'Label',
        disabled: true,
        buttonType: ButtonTypeEnum.Distructive,
        iconBefore: 'eye',
        iconBeforeWidth: '17',
        iconAfter: 'chevron-down',
        iconAfterWidth: '17'
      }}>
      {Template.bind({})}
    </Story>
</Canvas>

###### Component
```ts
export class ButtonComponent {
  @Input() label: string;
  @Input() large: boolean = false;
  @Input() disabled: boolean = false;
  @Input() iconBefore: string;
  @Input() iconBeforeWidth: string = 'auto';
  @Input() iconBeforeHeight: string = 'auto';
  @Input() iconAfter: string;
  @Input() iconAfterWidth: string = 'auto';
  @Input() iconAfterHeight: string = 'auto';
  @Input() buttonType: ButtonTypeEnum = ButtonTypeEnum.Primary;
}
```

###### Types
```ts
export enum ButtonTypeEnum {
  Primary = 'primary',
  Secondary = 'secondary',
  Tertiary = 'tertiary',
  Distructive = 'distructive'
}
```
