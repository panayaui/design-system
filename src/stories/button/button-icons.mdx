import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
import ButtonComponent from './button.component';
import {ButtonTypeEnum} from './button-page/button-type.enum';
import IconComponent from '../icons/icon.component';
import {IconsModule} from '../icons/icons.module';

<Meta title='Components/Button/WithIcons'
  component={ButtonComponent}
  subcomponents={IconComponent}
  decorators={[
    moduleMetadata({
      imports: [IconsModule]
  })
]} />

export const Template = (args) => <ButtonComponent {...args} />

# Button

###### Selector
```ts
  <p-button></p-button>
```

###### Button - Icon before (small)
<Canvas>
  <Story name="PrimaryIconBefore" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Primary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="SecondaryIconBefore" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Secondary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="TertiaryIconBefore" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Tertiary,
      iconBefore: 'eye',
      iconBeforeWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="DistructiveIconBefore" width="200px"
      args={{
        label: 'Label',
        buttonType: ButtonTypeEnum.Distructive,
        iconBefore: 'eye',
        iconBeforeWidth: '17'
      }}>
      {Template.bind({})}
    </Story>
</Canvas>

###### Button - Icon after (small)
<Canvas>
  <Story name="PrimaryIconAfter" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Primary,
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="SecondaryIconAfter" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Secondary,
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="TertiaryIconAfter" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Tertiary,
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="DistructiveIconAfter" width="200px"
      args={{
        label: 'Label',
        buttonType: ButtonTypeEnum.Distructive,
        iconAfter: 'chevron-down',
        iconAfterWidth: '17'
      }}>
      {Template.bind({})}
    </Story>
</Canvas>

###### Button - Icon before & after (small)
<Canvas>
  <Story name="PrimaryBothIcons" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Primary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="SecondaryBothIcons" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Secondary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="TertiaryBothIcons" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Tertiary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="DistructiveBothIcons" width="200px"
      args={{
        label: 'Label',
        buttonType: ButtonTypeEnum.Distructive,
        iconBefore: 'eye',
        iconBeforeWidth: '17',
        iconAfter: 'chevron-down',
        iconAfterWidth: '17'
      }}>
      {Template.bind({})}
    </Story>
</Canvas>

###### Disabled With Icons (small)
<Canvas>
  <Story name="PrimaryBothIconsDisabled" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Primary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17',
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
  <Story name="SecondaryBothIconsDisabled" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Secondary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17',
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
  <Story name="TertiaryBothIconsDisabled" width="200px"
    args={{
      label: 'Label',
      buttonType: ButtonTypeEnum.Tertiary,
      iconBefore: 'eye',
      iconBeforeWidth: '17',
      iconAfter: 'chevron-down',
      iconAfterWidth: '17',
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
  <Story name="DistructiveBothIconsDisabled" width="200px"
      args={{
        label: 'Label',
        buttonType: ButtonTypeEnum.Distructive,
        iconBefore: 'eye',
        iconBeforeWidth: '17',
        iconAfter: 'chevron-down',
        iconAfterWidth: '17',
        disabled: true
      }}>
      {Template.bind({})}
    </Story>
</Canvas>

###### Component
```ts
export default class ButtonComponent {
  @Input() label: string;
  @Input() large: boolean = false;
  @Input() disabled: boolean = false;
  @Input() iconBefore: string;
  @Input() iconBeforeWidth: string = 'auto';
  @Input() iconBeforeHeight: string = 'auto';
  @Input() iconAfter: string;
  @Input() iconAfterWidth: string = 'auto';
  @Input() iconAfterHeight: string = 'auto';
  @Input() buttonType: ButtonTypeEnum = ButtonTypeEnum.Primary;
}
```

###### Types
```ts
export enum ButtonTypeEnum {
  Primary = 'primary',
  Secondary = 'secondary',
  Tertiary = 'tertiary',
  Distructive = 'distructive'
}
```
