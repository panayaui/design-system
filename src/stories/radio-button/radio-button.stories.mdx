import {Meta, Story, Canvas, Preview} from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
import {CommonModule} from '@angular/common';
import {MatRadioModule} from '@angular/material/radio';
import {BrowserAnimationsModule} from '@angular/platform-browser/animations';
import RadioButtonComponent from './radio-button.component';

<Meta title='Components/RadioButton' component={RadioButtonComponent}
decorators={[
  moduleMetadata({
    imports: [CommonModule,
            MatRadioModule]
        })
]} />

# Radio Button

export const Template = (args) => <RadioButtonComponent {...args} />

###### Selector

```html
  <p-radio-button></p-input-numeric>
```

###### Off
<Canvas>
  <Story name="WithLabelOff" 
    args={{
      label: 'Label'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="WithLabelOffDisabled" 
    args={{
      label: 'Label',
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
  <Story name="NoLabelOff"
    args={{
      ariaLabel: 'Label'
    }}>
    {Template.bind({})}
  </Story>
  <Story name="NoLabelOffDisabled" 
    args={{
      ariaLabel: 'Label',
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### On
<Canvas>
  <Story name="WithLabelOn" 
    args={{
      label: 'Label',
      checked: true
    }}>
    {Template.bind({})}
  </Story>
  <Story name="WithLabelOnDisabled" 
    args={{
      label: 'Label',
      checked: true,
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
  <Story name="NoLabelOn"
    args={{
      ariaLabel: 'Label',
      checked: true
    }}>
    {Template.bind({})}
  </Story>
  <Story name="NoLabelOnDisabled" 
    args={{
      ariaLabel: 'Label',
      checked: true,
      disabled: true
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

###### Component
```ts
export default class InputNumericComponent implements OnInit {
  
}
```

